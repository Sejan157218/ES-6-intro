const FirstName = 'sejan';
const lastname = 'mahmud';
const fullName = FirstName + ' ' +lastname;

// with backcode ``
const fullNames = `${FirstName} ${lastname}`

console.log(fullName);
console.log(fullNames);

const array = ['sejan','sajal','mahmud'];
const num = 5;

const multiplication =`${num * array.length}`;
console.log(multiplication);

const multiLine = 'amr sunar bangla ami tumai valovashi'   +
'\n ami tumai valovashi' + 
'\n amr sunar bangla' 
console.log(multiLine);


// with backcode ``

const multiLineBackcode = `amr sunar bangla
 ami tumai valovashi
amr sunar bangla` 

console.log(multiLineBackcode);